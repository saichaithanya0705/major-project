# Model/Router AI-Slop Remediation Tasks

Scope: remaining AI-slop-like signals in the model/router hotspot cluster after the bridge-decomposition repairs already completed on 2026-05-16.

Source basis:
- `graphify-out/GRAPH_REPORT.md`
- `graphify_slop_scan.py` triage output
- direct source inspection of `models/models.py`, `models/routing_policy.py`, `models/router_backends.py`, `models/router_runtime.py`, and `models/rapid_orchestrator.py`

Status legend:
- `pending`: identified but not yet assigned
- `assigned`: handed to a subagent
- `running`: subagent actively working
- `returned`: subagent finished; main agent review pending
- `integrated`: reviewed and merged into the main line
- `rejected`: returned work not accepted
- `closed`: no longer needed because another fix eliminated the seam

## Subagent Board

| Agent | Scope | Ownership | Status | Merge Point |
|---|---|---|---|---|
| `Volta` | `models/routing_policy.py` seam inventory | read-only | `closed` | inventory folded into backlog |
| `Dewey` | `models/router_backends.py` seam inventory | read-only | `closed` | inventory folded into backlog |
| `Banach` | `models/router_runtime.py` + `models/models.py` runtime touchpoints | read-only | `closed` | inventory folded into backlog |
| `Hypatia` | `models/rapid_orchestrator.py` seam inventory | read-only | `closed` | inventory folded into backlog |
| `Wegener` | `router_backends` parser/client decomposition | `models/router_backends.py`, new parser/type module(s), dedicated tests | `closed` | validated and integrated |
| `Galileo` | `router_runtime` request-scope and protocol seam | `models/router_runtime.py`, minimal `models/models.py` touchpoints, dedicated tests | `closed` | validated and integrated |
| `Dirac` | `routing_policy` parser/guardrail boundary | `models/routing_policy.py`, new helper module(s), focused routing tests | `closed` | validated and integrated for the prompt/provider-validation slice |
| `Rawls` | duplicated `screen_context` runtime extraction | `models/rapid_orchestrator.py`, `models/rapid_plan_orchestrator.py`, shared step runtime, focused tests | `closed` | validated and integrated |
| `Newton` | routing JSON-salvage boundary | `models/routing_policy.py`, `models/routing_payload_parser.py`, focused routing tests | `closed` | validated and integrated |
| `Aquinas` | backend exception-boundary cleanup | `models/router_backends.py`, `models/openrouter_runtime.py`, focused backend tests | `closed` | validated and integrated |
| `Franklin` | router execution-policy extraction | `models/router_runtime.py`, one new execution-policy helper module if needed, focused runtime/bridge tests | `closed` | validated and integrated |
| `Mill` | next routing-policy seam selection | read-only survey of `models/routing_policy.py` and focused tests | `closed` | recommendation folded into backlog and next worker handoff |
| `Bohr` | routing intent/deictic seam extraction | `models/routing_policy.py`, one new intent-policy helper module if needed, `tests/test_routing_policy.py` only | `closed` | validated and integrated |
| `Euclid` | router provider-order policy extraction | `models/routing_policy.py`, `models/router_execution_policy.py`, one new provider-policy helper module if needed, focused routing/runtime tests | `closed` | validated and integrated |
| `Confucius` | next rapid-orchestrator seam selection | read-only survey of `models/rapid_orchestrator.py`, deps, and focused tests | `closed` | recommendation folded into backlog and next worker handoff |
| `Hume` | rapid completion/recovery policy extraction | `models/rapid_orchestrator.py`, one new rapid policy module if needed, focused chaining tests | `closed` | validated and integrated |
| `Descartes` | next rapid-orchestrator seam selection after MR-15 | read-only survey of `models/rapid_orchestrator.py`, deps, and focused tests | `closed` | recommendation folded into backlog and next worker handoff |
| `Russell` | delegated step execution runtime extraction | `models/rapid_orchestrator.py`, one new rapid step runtime module if needed, focused chaining/runtime tests | `closed` | validated and integrated |
| `Kepler` | `models.models` bridge-surface survey for `MR-16` | read-only | `closed` | recommendation reviewed and integrated locally |
| `Gibbs` | shared routing-contract survey for `MR-17` | read-only | `closed` | recommendation folded into next-seam backlog |
| `Euler` | direct-response finalization seam survey | read-only | `closed` | recommendation reviewed and integrated locally |
| `Tesla` | direct-response caller/test blast-radius survey | read-only | `closed` | recommendation reviewed and integrated locally |
| `Halley` | routing normalization seam survey | read-only | `closed` | recommendation reviewed and integrated locally |
| `Gauss` | routing normalization caller/test blast-radius survey | read-only | `closed` | recommendation reviewed and integrated locally |
| `Lovelace` | execution-surface/actionable-agent seam reference survey | read-only | `closed` | local extraction completed and validated before survey result returned |
| `Bacon` | remediation/task-board survey for execution-surface seam | read-only | `closed` | helper hit usage limits; local docs update completed by main agent |
| `Hooke` | question-classification seam reference survey | read-only | `closed` | local extraction completed and validated before survey result returned |
| `Beauvoir` | route/plan payload soft-contract survey (`JsonObject` + `direct_response_args`) | read-only | `closed` | helper hit usage limits; local survey and integration completed by main agent |

## Canonical Task List

| ID | Priority | Module | Issue | Permanent Fix Direction | Focused Validation | Status |
|---|---|---|---|---|---|---|
| `MR-01` | P0 | `models/routing_policy.py` | Intent classification, prompt parsing, guardrails, direct-response formatting, and router finalization are still co-located in one oversized module. | Split routing policy into focused boundaries for intent classification, payload parsing, guardrails/finalization, and prompt formatting. | `tests/test_routing_policy.py`, `tests/test_routing_contracts.py`, `tests/test_router_chaining.py` | `pending` |
| `MR-01A` | P1 | `models/routing_policy.py` | Latest-request prompt parsing lived as inline residue inside the routing-policy module. | Extract shared router prompt parsing into `models/routing_prompt_parser.py` and route policy callers through it. | `tests/test_routing_policy.py`, `tests/test_browser_resume_route_boundary.py`, `tests/test_routing_contracts.py` | `integrated` |
| `MR-01B` | P1 | `models/routing_policy.py` + extracted response helpers | Shared text cleanup and direct-response repeat/finalization heuristics still leaked out of `routing_policy` into direct-answer and rapid runtimes. | Extract shared text normalization into `models/text_normalization.py`, move repeat-artifact/direct-response finalization policy into `models/direct_response_policy.py`, and rewire runtime callers directly to those owning modules. | `tests/test_direct_response_policy_boundary.py`, `tests/test_routing_contracts.py`, focused direct-response/router slices | `integrated` |
| `MR-01C` | P1 | `models/routing_policy.py` + extracted normalization helpers | Router-decision normalization and refusal-handling still lived inside `routing_policy` even though runtime/model callers only need a backend-response normalization boundary. | Extract route normalization and plan normalization into `models/routing_decision_policy.py`, keep `routing_policy` as a compatibility alias, and rewire runtime/bridge callers to the owning module. | `tests/test_routing_policy_module_boundaries.py`, `tests/test_routing_contracts.py`, focused router/runtime slices | `integrated` |
| `MR-01D` | P2 | `models/routing_policy.py` + step bookkeeping helpers | Route task-text cleanup and repeated-step signature generation were still imported through `routing_policy` by rapid deps and agent-step execution. | Extract step identity helpers into `models/routing_step_identity.py`, keep `routing_policy` aliases for compatibility, and rewire internal runtime callers to the owning module. | `tests/test_routing_policy_module_boundaries.py`, focused routing/chaining slices | `integrated` |
| `MR-01E` | P2 | `models/routing_policy.py` + extracted surface policy helpers | Execution-surface heuristics and actionable-agent selection still lived inside `routing_policy` even though guardrail enforcement and execution intent classification only need a stable surface-policy boundary. | Extract execution-surface markers plus `is_execution_request(...)`, `is_window_management_request(...)`, `requires_desktop_control_surface(...)`, and `choose_actionable_agent(...)` into `models/routing_surface_policy.py`; keep `routing_policy` compatibility aliases and rewire guardrail defaults to the owning module. | `tests/test_routing_policy_module_boundaries.py`, `tests/test_routing_contracts.py`, focused routing/chaining slices | `integrated` |
| `MR-01F` | P2 | `models/routing_policy.py` + extracted question-classification helpers | Visual screen-question detection plus direct/web QA wrappers still lived inside `routing_policy` even though generic intent rules already existed in a separate intent-policy module. | Extract `is_visual_explanation_request(...)`, `is_direct_qa_request(...)`, and `is_web_qa_request(...)` into `models/routing_question_policy.py`; keep `routing_policy` compatibility aliases and rewire rapid/guardrail callers to the owning question-policy module. | `tests/test_routing_policy_module_boundaries.py`, `tests/test_routing_contracts.py`, focused routing/direct-answer/chaining slices | `integrated` |
| `MR-01G` | P2 | `models/routing_policy.py` + `models/routing_guardrails.py` | The rapid orchestrator still reached through `routing_policy` for the route-only guardrail adapter, and the guardrail actionable-agent callable still widened `RouteAgentName` back to plain `str`. | Add a route-only adapter to `models/routing_guardrails.py`, rewire rapid deps to the owning guardrail module, keep `routing_policy` compatibility aliases, and tighten the guardrail callable contract to preserve `RouteAgentName`. | `tests/test_routing_policy_module_boundaries.py`, `tests/test_routing_contracts.py`, focused guardrail/chaining slices | `integrated` |
| `MR-02` | P0 | `models/routing_policy.py` | JSON object salvage still relies on broad catches and silent fallback. | Replace loose salvage with a typed parser boundary that reports parse modes/failures explicitly. | `tests/test_routing_policy.py`, `tests/test_routing_contracts.py` | `integrated` |
| `MR-03` | P1 | `models/routing_policy.py` | Provider-order policy still lives beside unrelated routing heuristics. | Extract provider-order / provider-eligibility policy into a dedicated boundary shared with runtime callers if appropriate. | `tests/test_routing_policy.py`, `tests/test_router_provider_failures.py`, `tests/test_router_chaining.py` | `integrated` |
| `MR-03A` | P1 | `models/routing_policy.py` | Unknown `router_provider` values could silently fall through the generic provider-order path. | Reject unknown provider values immediately instead of silently using fallback ordering. | `tests/test_routing_policy.py`, `tests/test_router_chaining.py` | `integrated` |
| `MR-04` | P1 | `models/routing_policy.py` | Helper APIs still accept/return `dict[str, Any]` across most routing payload boundaries. | Introduce typed payload models or `TypedDict`/protocol contracts for router decisions and plan decisions. | `tests/test_routing_contracts.py`, `tests/test_routing_policy.py` | `pending` |
| `MR-04A` | P1 | `models/routing_policy.py` | Over-broad deictic string heuristics force too many prompts out of direct/web QA. | Isolate intent classification into ordered rules and remove bare deictics as global blockers unless stronger screen-dependence evidence exists. | `tests/test_routing_policy.py` | `integrated` |
| `MR-04B` | P1 | `models/routing_policy.py` | Guardrail rewrites are opaque and lose rewrite provenance. | Return typed guardrail rewrite metadata (`rewritten_from`, `rewrite_reason`) from a dedicated guardrail module. | `tests/test_routing_policy.py`, `tests/test_browser_resume_route_boundary.py`, `tests/test_router_chaining.py` | `integrated` |
| `MR-05` | P0 | `models/router_backends.py` | Provider HTTP clients and tool-call/text parsing live in the same large file. | Split backend I/O from text/tool-call parsing into separate modules with narrow responsibilities. | `tests/test_router_backends_boundary.py`, `tests/test_router_provider_failures.py`, `tests/test_router_chaining.py` | `integrated` |
| `MR-06` | P0 | `models/router_backends.py` | Normalized tool-call parsing still depends on loose `Any` payloads and JSON salvage paths. | Add typed normalized tool-call structures and deterministic parsing helpers with explicit failure behavior. | `tests/test_router_backends_boundary.py`, `tests/test_routing_contracts.py` | `integrated` |
| `MR-07` | P1 | `models/router_backends.py` | Backend validation/error paths still broad-catch transport/parse failures. | Narrow exception handling to the expected request/JSON/runtime failures and preserve actionable diagnostics. | `tests/test_router_provider_failures.py`, `tests/test_router_backends_boundary.py` | `integrated` |
| `MR-07A` | P1 | `models/router_backends.py` + adjacent callers | Backend wrappers and direct vision callers still rebuild provider state through loose attribute bags. | Introduce a typed provider config/client boundary shared by wrappers and direct callers. | `tests/test_models_bridge_boundaries.py`, `tests/test_router_backends_boundary.py`, relevant vision focused slice | `integrated` |
| `MR-08` | P0 | `models/router_runtime.py` | Module-global provider failure state is still shared across requests. | Replace module-global last-failure storage with request-scoped failure capture passed through the runtime boundary. | `tests/test_router_provider_failures.py`, `tests/test_router_chaining.py`, `tests/test_models_bridge_boundaries.py` | `integrated` |
| `MR-09` | P0 | `models/router_runtime.py` | Runtime functions still depend on a soft `model: Any` contract. | Introduce a typed router runtime protocol for the model/provider contract. | `tests/test_models_bridge_boundaries.py`, `tests/test_router_provider_failures.py`, `tests/test_router_chaining.py` | `integrated` |
| `MR-10` | P1 | `models/router_runtime.py` | Provider loop still broad-catches errors across different failure modes. | Separate timeout, configuration, provider-call, and normalization failures into explicit branches with typed recording. | `tests/test_router_provider_failures.py`, `tests/test_router_chaining.py` | `integrated` |
| `MR-11` | P1 | `models/router_runtime.py` | Provider selection, timeout policy, and execution loop are still coupled. | Extract provider-execution policy from the loop so selection logic can be tested independently of async execution. | `tests/test_router_provider_failures.py`, `tests/test_routing_policy.py` | `integrated` |
| `MR-11A` | P1 | `models/models.py` + session state boundary | `models.models` still exposes a stale default-session rapid-history alias. | Remove or deprecate `_RAPID_CONVERSATION_HISTORY` and force callers/tests onto explicit session-scoped history APIs. | focused `tests/test_router_chaining.py` history slice | `integrated` |
| `MR-12` | P0 | `models/rapid_orchestrator.py` | The main rapid loop still mixes direct-answer fast path, route loop control, recovery policy, logging, and screen-context handling. | Split the orchestrator into focused runtime helpers for direct fast path, route iteration, recovery/completion policy, and screen-context handling. | `tests/test_router_chaining.py`, `tests/test_models_bridge_boundaries.py`, `tests/test_rapid_state_boundary.py` | `integrated` |
| `MR-12A` | P1 | `models/rapid_orchestrator.py` + `models/rapid_direct_answer_flow.py` | The direct-QA fast path still lived inline in `run_rapid_request()`. | Extract the direct-QA orchestration flow into a dedicated runtime helper with focused boundary tests. | `tests/test_rapid_direct_answer_flow.py`, `tests/test_router_chaining.py` | `integrated` |
| `MR-13` | P0 | `models/rapid_orchestrator.py` | Dependency surface is still callable-heavy and `Any`-heavy even after `RapidOrchestratorDeps` extraction. | Replace loose callable bundles with typed protocols/data contracts where the runtime really depends on stable behavior. | `tests/test_models_bridge_boundaries.py`, `tests/test_router_chaining.py`, `tests/test_routing_contracts.py` | `integrated` |
| `MR-14` | P1 | `models/rapid_orchestrator.py` | Broad catches still wrap direct answer, router execution, and screen-context subflows. | Narrow exception handling per subflow and centralize failure mapping/logging without swallowing structural errors. | `tests/test_router_chaining.py`, `tests/test_rapid_state_boundary.py` | `integrated` |
| `MR-15` | P1 | `models/rapid_orchestrator.py` | Completion and recovery policy remains embedded in the orchestration module instead of a dedicated policy boundary. | Extract completion/recovery decision logic into a policy module with targeted behavior tests. | `tests/test_router_chaining.py`, `tests/test_routing_contracts.py` | `integrated` |
| `MR-15A` | P1 | `models/rapid_orchestrator.py` + `models/rapid_plan_orchestrator.py` | `screen_context` step execution is duplicated across legacy and plan runtimes. | Extract a shared `run_screen_context_step(...)` runtime used by both orchestrators. | `tests/test_router_chaining.py`, `tests/test_models_bridge_boundaries.py` | `integrated` |
| `MR-16` | P1 | `models/models.py` + runtime modules | `GeminiModel` still re-exports many runtime boundaries and remains the compatibility bridge for a wide API surface. | Review remaining bridge exports and shrink `models.models` to compatibility wrappers plus construction only where legacy API requires it. | `tests/test_models_bridge_boundaries.py`, `tests/test_router_chaining.py` | `integrated` |
| `MR-16A` | P1 | `models/models.py` + `models/request_entrypoint.py` | `call_gemini()` still owned request-start / request-crash lifecycle work inside the compatibility bridge. | Extract the public request entrypoint into a dedicated helper and keep `models.models.call_gemini()` as a thin bridge wrapper. | `tests/test_models_bridge_boundaries.py`, `tests/test_router_chaining.py`, `tests/model_test.py` | `integrated` |
| `MR-17A` | P1 | `models/rapid_orchestrator.py` + `models/routing_policy.py` | `ScreenContextPayload` existed but widened back to raw dicts across the rapid/router handoff. | Keep `latest_screen_context` and the related routing-policy helpers typed end-to-end as `ScreenContextPayload`. | `tests/test_routing_contracts.py`, `tests/test_screen_context_execution_runtime.py`, `tests/test_router_chaining.py` | `integrated` |
| `MR-17B` | P1 | `models/router_backend_parsing.py` + `models/router_backends.py` + `models/router_runtime.py` | Provider router parsing still collapsed structured provider output into single-route decisions, so plan payloads could not cross the backend/runtime seam. | Preserve non-empty structured router JSON at the backend boundary and let the runtime normalize it into a route or plan using distinct typed payload contracts. | `tests/test_router_backend_parser_boundary.py`, `tests/test_router_backends_boundary.py`, `tests/test_routing_policy.py`, `tests/test_router_provider_failures.py` | `integrated` |
| `MR-17C` | P1 | `models/rapid_orchestrator_contracts.py` + `models/routing_payload_parser.py` + adjacent step/orchestrator modules | Rapid step payload fields (`tool_calls`, `sources`, `artifacts`, `orchestrator_context`) still widened into anonymous dict/list payloads even after the routing and plan boundaries were extracted. | Introduce a shared `models/rapid_step_payloads.py` module for typed tool-call, source, artifact, step-metadata, and orchestration-context payloads; rewire rapid contracts, route payloads, and connected runtimes to reuse those shared payload types. | `tests/test_rapid_orchestrator_contracts.py`, `tests/test_routing_contracts.py`, `tests/test_orchestrator_adapters.py`, `tests/test_rapid_state_boundary.py`, focused chaining slice | `integrated` |
| `MR-17D` | P1 | `models/routing_payload_parser.py` + `models/router_backend_parsing.py` + `models/orchestrator_adapters.py` | The route/plan seam still had two competing JSON-object contracts: backend parsing normalized nested payloads, while routing payloads and direct-response metadata still widened back to loose `dict[str, Any]` shapes. | Introduce one shared JSON-object coercion boundary, reuse it from routing payload parsing and backend parsing, type `direct_response_args` as a named shared payload, and reject non-JSON direct-response metadata at the adapter/planner edge. | `tests/test_routing_contracts.py`, `tests/test_orchestrator_adapters.py`, `tests/test_orchestrator_planner.py`, `tests/test_router_backend_parser_boundary.py`, `tests/test_router_backends_boundary.py`, focused router/chaining slice | `integrated` |
| `MR-17E` | P1 | `models/contracts.py` + direct-route normalization/execution layers | Direct routes still had two competing text carriers: `response_text` and `direct_response_args["text"]`, so guardrails and executors had to keep fallback logic for both shapes. | Extend `RouteDecision` to preserve auxiliary direct-response kwargs, promote any direct text out of `direct_response_args["text"]` at normalization/copy boundaries, preserve those kwargs through guardrail copies, and make executors consume only `response_text` for the answer body. | `tests/test_router_backend_parser_boundary.py`, `tests/test_orchestrator_adapters.py`, `tests/test_orchestrator_planner.py`, `tests/test_router_chaining.py`, broader router/orchestrator slice | `integrated` |
| `MR-17F` | P1 | `models/routing_payload_parser.py` + `models/routing_guardrails.py` + `models/orchestrator_quality.py` | Route payload copying and “work text” derivation were still hand-rolled in multiple layers, so planner quality and guardrail copies could drift away from the payload owner’s normalization rules. | Extract shared `copy_route_payload(...)` and `route_payload_work_text(...)` ownership into the route payload boundary, and rewire guardrails plus planner quality to consume those helpers instead of rebuilding route semantics locally. | `tests/test_routing_payload_parser_boundary.py`, `tests/test_orchestrator_quality.py`, `tests/test_orchestrator_planner.py`, broader routing/orchestrator slice | `integrated` |
| `MR-17` | P1 | Cross-cutting router/model slice | Typed routing payloads are inconsistent across policy, runtime, backends, and orchestrator. | Introduce shared routing contracts reused by all router layers instead of repeated `dict[str, Any]` conventions. | `tests/test_routing_contracts.py`, connected router/model tests | `pending` |
| `MR-18` | P2 | Cross-cutting router/model slice | Focused tests exist, but each new extraction should pin module ownership more explicitly. | Add or extend boundary tests for every new runtime/policy/parser module introduced during this remediation wave. | boundary tests + affected focused slice | `pending` |

## Execution Order

1. `MR-01` to `MR-04` (`routing_policy`)
2. `MR-05` to `MR-07` (`router_backends`)
3. `MR-08` to `MR-11A` (`router_runtime` and request/session scope)
4. `MR-12` to `MR-15A` (`rapid_orchestrator` and plan/runtime duplication)
5. `MR-16` to `MR-18` (cross-cutting consolidation and boundary hardening)

## Notes

- This backlog is intentionally granular so each seam can be assigned to a bounded subagent without overlapping write ownership.
- Status changes are authoritative only after main-agent review and integration.
- If a higher-priority fix eliminates a lower-priority seam, mark the lower item `closed` with the reason in the audit report.
